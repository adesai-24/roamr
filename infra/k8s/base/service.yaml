apiVersion: v1
kind: Service
metadata:
  name: roamr-web
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      # Named rather than numeric so the container port can move without
      # touching the Service.
      targetPort: http
      protocol: TCP
