apiVersion: v1
kind: ServiceAccount
metadata:
  name: roamr-web
# The app talks only to Supabase over HTTPS and never calls the Kubernetes API,
# so there is no reason to hand its container a cluster credential.
automountServiceAccountToken: false
